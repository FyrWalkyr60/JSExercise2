console.log(course.name);
console.log(course.teachers[1])
console.log(course.students[0].name[0])

if (course.students.name[x].computer.OS==course.students.name[x].computer.OS[osx]) {
    OSList.push (course.students.name[x]);


    while  (course.students[x] <3) {
        console.log(course.students[x].name)
      if (course.students[x].name.computer.OS==course.students[x].name.computer.OS[osx]) {
          nameList.push (course.students[x].name);
          x++
        }
      }